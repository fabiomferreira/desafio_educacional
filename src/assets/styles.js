const colors = {
  primary: '#000',
  secondary: '#b82a28',
  white: '#fff',
  shadow: '#eee',
  border: '#ccc',
  label: '#333',
  background: '#edeff3'
};

const spacing = {
  base: '1rem',
  small: '0.5rem',
};

const boxShadow = {
  base: `1px 1px 4px ${colors.shadow}`,
};

const borderRadius = '4px';

const border = {
  base: `1px solid ${colors.border}`,
};

const fontSize = {
  label: '12px',
  info: '14px',
};

export {
  colors,
  spacing,
  boxShadow,
  borderRadius,
  border,
  fontSize,
};
